# Interface WireGuard du SIÈGE — À COMPLÉTER
# LAN siège : 192.168.10.0/24 - IP publique : 203.0.113.10
# LAN filiale : 192.168.20.0/24 - IP publique : 198.51.100.20
# Réseau de tunnel : 10.99.0.0/24

[Interface]
# TODO : adresse de tunnel du siège + port d'écoute + clé privée
# Address = 10.99.0.1/24
# ListenPort = 51820
# PrivateKey = <CLÉ PRIVÉE DU SIÈGE>

# TODO : règles de transit firewall (PostUp / PostDown)
# PostUp   = iptables -A FORWARD -i wg0 -j ACCEPT
# PostDown = iptables -D FORWARD -i wg0 -j ACCEPT

[Peer]
# TODO : pair = passerelle filiale
# PublicKey = <CLÉ PUBLIQUE DE LA FILIALE>
# Endpoint = 198.51.100.20:51820
# AllowedIPs = <adresse de tunnel filiale>, <LAN filiale>