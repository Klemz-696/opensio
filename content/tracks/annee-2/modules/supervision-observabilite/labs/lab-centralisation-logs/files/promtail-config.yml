# promtail-config.yml - Configuration Promtail

server:
  http_listen_port: 9080
  grpc_listen_port: 0

# TODO: Configurer le fichier de positions (filename: /tmp/positions.yaml)

# TODO: Configurer l'URL client Loki (http://loki:3100/loki/api/v1/push)

scrape_configs:
  # TODO: Configurer le job nginx-access (/var/log/nginx/*.log, labels job et env, pipeline_stages JSON)
