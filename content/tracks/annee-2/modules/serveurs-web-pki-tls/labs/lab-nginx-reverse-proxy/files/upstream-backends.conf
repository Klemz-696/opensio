# Upstream Nginx — Pool de backends OpenSIO (À COMPLÉTER)
# Les backends écoutent sur le port 8080 sur le réseau interne 10.10.20.0/24.

# TODO : créer le bloc upstream opensio_backend avec :
#   - la stratégie least_conn (backend le moins chargé)
#   - le backend principal 10.10.20.11:8080 avec max_fails=3 fail_timeout=30s
#   - le backend principal 10.10.20.12:8080 avec max_fails=3 fail_timeout=30s
#   - le backend de secours 10.10.20.13:8080 marqué backup
#   - des connexions persistantes : keepalive 16

# upstream opensio_backend {
#     least_conn;
#     server 10.10.20.11:8080 max_fails=3 fail_timeout=30s;
#     server 10.10.20.12:8080 max_fails=3 fail_timeout=30s;
#     server 10.10.20.13:8080 backup;
#     keepalive 16;
# }