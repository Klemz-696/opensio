# Reverse Proxy Nginx — Virtual Host OpenSIO (À COMPLÉTER)
# Objectif : rediriger HTTP vers HTTPS, relayer vers l'upstream et
# transmettre les en-têtes client.

server {
    listen 80;
    server_name opensio.home.lan;

    # TODO 1 : rediriger tout le trafic HTTP vers HTTPS (301 permanent)
    # return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name opensio.home.lan;

    ssl_certificate     /etc/nginx/pki/opensio.home.lan.crt;
    ssl_certificate_key /etc/nginx/pki/opensio.home.lan.key;

    location / {
        # TODO 2 : relayer vers l'upstream opensio_backend
        # proxy_pass http://opensio_backend;

        # TODO 3 : transmettre la réalité client au backend
        # (Host, X-Real-IP, X-Forwarded-For, X-Forwarded-Proto)

        # TODO 4 : activer HTTP/1.1 et les connexions persistantes
        # (proxy_http_version 1.1 et Connection "")
    }
}