# Upstream Nginx — SOLUTION INVALIDE : un seul backend et pas de stratégie
# Aucune répartition de charge, aucun redondance : SPOF côté backend.

upstream opensio_backend {
    server 10.10.20.11:8080;
}