# En-têtes de sécurité HTTP — À COMPLÉTER
# À inclure dans le bloc server HTTPS : include /etc/nginx/snippets/security-headers.conf;

# TODO 1 : HSTS d'un an avec includeSubDomains et drapeau always
# add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;

# TODO 2 : interdiction du sniffing MIME
# add_header X-Content-Type-Options "nosniff" always;

# TODO 3 : protection anti-clickjacking
# add_header X-Frame-Options "DENY" always;

# TODO 4 : politique de référent et politique d'autorisations navigateur
# add_header Referrer-Policy "strict-origin-when-cross-origin" always;
# add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;