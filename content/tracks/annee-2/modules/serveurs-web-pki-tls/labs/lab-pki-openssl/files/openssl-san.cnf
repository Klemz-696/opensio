# Extensions OpenSSL pour le certificat serveur (À COMPLÉTER)
# Rappel : le SAN est la source de vérité des noms couverts pour les navigateurs.

authorityKeyIdentifier = keyid, issuer
basicConstraints       = CA:FALSE
keyUsage               = digitalSignature, keyEncipherment
extendedKeyUsage       = serverAuth
subjectAltName         = @alt_names

[alt_names]
# TODO : couvrir opensio.home.lan et www.home.lan
# DNS.1 = opensio.home.lan
# DNS.2 = www.home.lan